# Dependencies to bundle into plugin/webapi_lib for MO2's embedded Python.
# Used by CI to populate webapi_lib/ before creating the release zip.
# Install with: pip install -r plugin/requirements-bundle.txt -t plugin/webapi_lib

# MO2 WebAPI Plugin Dependencies

# WebSocket support
websockets>=16.0.0

# Qt bindings (usually provided by MO2)
PyQt6>=6.0.0

fastapi>=0.135.1
uvicorn>=0.41.0
pydantic>=2.12.5
pydantic-core>=2.41.5
